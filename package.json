{
  "name": "sql-curriculum",
  "private": true,
  "scripts": {
    "test": "docker compose -f db/docker-compose.yml down -v && docker compose -f db/docker-compose.yml up -d --wait && jest",
    "practice": "docker compose -f db/docker-compose.yml down -v && docker compose -f db/docker-compose.yml up -d --wait && ts-node server.ts"
  },
  "devDependencies": {
    "@types/jest": "^29",
    "@types/pg": "^8",
    "jest": "^29",
    "pg": "^8",
    "ts-jest": "^29",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  }
}
